# Configuration Files

Hydra configuration files for reproducing NerVE experiments.

Place your `config.yaml` and any override configs here.
The training script expects configs at `configs/config.yaml`.
